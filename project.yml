name: Cursed
options:
  bundleIdPrefix: com.cursed
  deploymentTarget:
    macOS: "14.0"
  xcodeVersion: "16.0"
  minimumXcodeGenVersion: "2.0"
targets:
  Cursed:
    type: application
    platform: macOS
    sources:
      - Cursed
    settings:
      base:
        SWIFT_VERSION: "5"
        PRODUCT_BUNDLE_IDENTIFIER: com.cursed.browser
        MACOSX_DEPLOYMENT_TARGET: "14.0"
        GENERATE_INFOPLIST_FILE: YES
        PRODUCT_NAME: Cursed
