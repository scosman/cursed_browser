import SwiftUI

@main
struct CursedApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
                .frame(minWidth: 600, minHeight: 430)
        }
        .defaultSize(width: 1100, height: 765)
    }
}
